{
  "name": "rss-reader",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "bun run scripts/dev.ts",
    "build": "bun run backend:build && bun run --filter @rss-reader/desktop build",
    "package": "bun run build && bun run scripts/package.ts",
    "test": "bun test && cd backend && go test ./...",
    "lint": "bun run --filter '*' lint",
    "backend:build": "mkdir -p apps/desktop/resources/bin && cd backend && go build -o ../apps/desktop/resources/bin/rss-backend ./cmd/desktop",
    "backend:test": "cd backend && go test ./...",
    "backend:all": "bash scripts/build-backend-all.sh",
    "backend:stage": "bash scripts/stage-backend.sh"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}

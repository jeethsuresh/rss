# Sports F1 (OpenF1) implementation plan

## Tasks

1. Domain types + OpenF1 client (`ListYears`, `ListRaces`, `RaceDetail`) + smoke test.
2. `SportsService` F1 methods + watch/poll; wire client in `cmd/desktop`.
3. IPC methods + shared types + preload.
4. `sportsRegistry` F1 `available: true`; SportsView F1 sidebar + list + detail.
5. Build backend, run Go/UI checks, commit.
